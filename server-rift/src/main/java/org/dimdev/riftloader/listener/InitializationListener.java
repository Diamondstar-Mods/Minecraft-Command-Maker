package org.dimdev.riftloader.listener;

/**
 * Local stub of the RiftLoader InitializationListener interface for compilation.
 * At runtime, RiftLoader provides the actual interface and calls onInitialization().
 */
public interface InitializationListener {
    void onInitialization();
}

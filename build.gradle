plugins {
    id 'fabric-loom' version "${loom_version}"
    id 'maven-publish'
}

allprojects {
    apply plugin: 'fabric-loom'
    apply plugin: 'maven-publish'

    version = project.mod_version
    group = 'io.github.diamondstar_mods'

    repositories {
        mavenCentral()
        maven { url 'https://maven.fabricmc.net/' }
        maven { url 'https://maven.quiltmc.org/repository/release' }
    }

    dependencies {
        // Minecraft and mappings
        minecraft "com.mojang:minecraft:${project.minecraft_version}"
        mappings "net.fabricmc:yarn:${project.yarn_mappings}:v2"

        // Fabric Loader and API (for Fabric modules)
        modImplementation "net.fabricmc:fabric-loader:${project.loader_version}"
        modImplementation "net.fabricmc.fabric-api:fabric-api:${project.fabric_version}"

        // Optional: JSON parsing
        implementation "com.google.code.gson:gson:2.10.1"
    }

    // Quilt-specific dependencies for quilt modules
    if (project.name.contains('quilt')) {
        dependencies {
            // Quilt Loader
            modImplementation "org.quiltmc:quilt-loader:${project.quilt_loader_version}+${project.quilt_minecraft_version}"

            // Quilted Fabric API (for Minecraft 1.21.9)
            modImplementation "org.quiltmc.quilted-fabric-api:quilted-fabric-api:${project.quilted_fabric_api_version}+${project.quilt_minecraft_version}"

            // Quilt Standard Libraries (for Minecraft 1.21.9)
            modImplementation "org.quiltmc:qsl:${project.quilt_standard_libraries_version}+${project.quilt_minecraft_version}"
        }
    }

    tasks.withType(JavaCompile).configureEach {
        it.options.release = 21
    }

    java {
        withSourcesJar()
        sourceCompatibility = JavaVersion.VERSION_21
        targetCompatibility = JavaVersion.VERSION_21
    }
}

subprojects {
    loom {
        mods {
            "modid" {
                sourceSet sourceSets.main
            }
        }
    }

    processResources {
        inputs.property "version", project.version
        filesMatching("fabric.mod.json") {
            expand "version": inputs.properties["version"]
        }
    }

    jar {
        from("LICENSE") {
            rename { "CMDMakerFabric" }
        }
    }

    publishing {
        publications {
            create("mavenJava", MavenPublication) {
                artifactId = project.archives_base_name + (project.name == 'client' ? '-client' : '-server')
                from components.java
            }
        }
        repositories {
            maven {
                name = "GitHubPackages"
                url = uri("https://maven.pkg.github.com/${System.getenv('GITHUB_REPOSITORY')}")
                credentials {
                    username = System.getenv("USERNAME")
                    password = System.getenv("TOKEN")
                }
            }
        }
    }
}
